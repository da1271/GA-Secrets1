<%= @post.title %>

<%= @post.content %>
<% unless @post.anonymous %>
  <%= @post.user.name %>
<% end %>

<% if current_user.id == @post.user_id %>
<br>
</br>
<%= link_to "Edit", edit_post_path %>


<%= link_to "Delete post!", @post, method: :delete, data: { confirm: "Are you sure you want to delete this bean?"} %>



<% end %>
